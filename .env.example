# Example environment variables for RedzExchange UI
# Copy this to .env.local (or set in your hosting provider) and replace placeholders.
# IMPORTANT: Do NOT commit your real API keys to the repository.

# Alchemy API key for Solana RPC (optional). If set, the app will use:
# https://solana-mainnet.g.alchemy.com/v2/<KEY> (or devnet equivalent)
# Client-side (public) keys should be set as NEXT_PUBLIC_ALCHEMY_API_KEY when
# appropriate for read-only RPC access. Do NOT commit private keys.
NEXT_PUBLIC_ALCHEMY_API_KEY=YOUR_ALCHEMY_PUBLIC_RPC_KEY_HERE

# Primary network: mainnet | devnet | testnet
NEXT_PUBLIC_NETWORK=mainnet

# Optional: override RPC URL entirely
# NEXT_PUBLIC_RPC_URL=https://your-custom-rpc

# App identifiers
NEXT_PUBLIC_PROGRAM_ID=9HiX1zn36tRsmqJp2F1sGFNVFimoVcbe9JMGSUo9LsiV
NEXT_PUBLIC_APP_NAME=RedzExchange
NEXT_PUBLIC_APP_VERSION=1.0.0

# Feature flags (example)
# NEXT_PUBLIC_ENABLE_LOCAL_WALLET_GENERATION=true
# NEXT_PUBLIC_ENABLE_QR=true

# NOTE: This file is an example. For local development create a .env.local file
# and add it to .gitignore. For deployments (Vercel/Netlify/etc.) set the
# environment variables in your project's dashboard instead of committing them.

# Server-side (SECRET) variables for programmatic wallet creation via Alchemy
# These must be set in your deployment environment only (do NOT expose them to clients)
# ALCHEMY_API_KEY is your secret server-side API key.
# ALCHEMY_WALLET_API_URL is the full REST endpoint for programmatic wallet creation
# as documented by Alchemy (example value depends on Alchemy docs).
# ALCHEMY_API_KEY=YOUR_SERVER_ALCHEMY_API_KEY_HERE
# ALCHEMY_WALLET_API_URL=https://api.alchemy.com/v1/wallets